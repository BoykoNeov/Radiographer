# --- Python ---
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
*.egg-info/

# --- Node / JS ---
node_modules/
dist/
build/
.parcel-cache/
.vite/
npm-debug.log*
yarn-error.log*

# Generated runtime archive (engine + datasets) the browser fetches at boot —
# derived from committed source data; built on demand via web/scripts/build-archive.mjs.
web/public/radiographer-runtime.zip

# --- OS / editor ---
.DS_Store
Thumbs.db
Desktop.ini
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# --- Claude Code (commit shared CLAUDE.md / commands; ignore local-only) ---
.claude/settings.local.json

# --- Pyodide (uncomment only if you vendor the runtime instead of CDN) ---
# pyodide/

# --- Project data ---
# The processed, bundled §7 JSON datasets ARE committed — they are the project.
# Ignore only large RAW upstream dumps (NIST XCOM, ENSDF, ORIGEN) you process
# into those tables.
data/raw/
*.tmp
# data/build/ holds the dataset build scripts (source code, not JS build output) —
# re-include it despite the generic `build/` ignore in the Node section above.
!data/build/
!data/build/**

# --- SCK-CEN spent-fuel CSV (370 MB, CC-BY; build extracts compact JSON) ---
data/vendor/sckcen_sf/*.csv
