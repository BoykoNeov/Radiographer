[tool.pytest.ini_options]
# Make the repo root importable so `import engine` works without installation.
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py313"
